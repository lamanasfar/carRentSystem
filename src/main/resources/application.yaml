spring:
  application:
    name: carRentSystem

  datasource:
    url: jdbc:postgresql://localhost:5432/carappdb
    username: postgres
    password: lyamanava048
    driver-class-name: org.postgresql.Driver

  jpa:
    hibernate:
      ddl-auto: validate
    show-sql: true
    properties:
      hibernate:
        format_sql: true
    open-in-view: false

  liquibase:
    enabled: true
    change-log: classpath:liquibase/changelog-master.yml
